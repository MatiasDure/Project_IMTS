using System.Collections;
using UnityEngine;
using UnityEngine.TestTools;
using NUnit.Framework;

public class FishSwimmingInteractionTest
{
	/*
	[UnityTest]
	public IEnumerator PlayMode_FishSwimmingInteractionTest_SmoothSpeedChangeCoroutine()
	{
		FishSwimmingInteraction fishSwimmingInteraction = new GameObject().AddComponent<FishSwimmingInteraction>();

		float originalSpeed = fishSwimmingInteraction._moveSpeed = 0.01f;
		float targetMoveSpeed = 0.035f;
		float smoothChangeDuration = 0.5f;

		fishSwimmingInteraction.StartCoroutine
			(fishSwimmingInteraction.SmoothSpeedChangeCoroutine(originalSpeed, targetMoveSpeed, smoothChangeDuration));

		Assert.AreEqual(targetMoveSpeed, fishSwimmingInteraction._moveSpeed);

		yield return null;
	}
	*/
}